// Array Creation

// There are serveral ways to create an array including:

// 1 - Using the array literal
let a1 = [1, 2, 3];

// 2 - Using the Array constructor (but this is not recommended)
let a2 = new Array(1, 2, 3);
