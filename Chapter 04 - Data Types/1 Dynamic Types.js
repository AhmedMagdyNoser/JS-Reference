// JavaScript is a dynamically-typed language (a weakly-typed language).

// The interpreter assigns variables a type at runtime based on the variable's value at the time.

let dynamicVariable;

dynamicVariable = 10; // number
dynamicVariable = 'Ali'; // string
dynamicVariable = true; // boolean
