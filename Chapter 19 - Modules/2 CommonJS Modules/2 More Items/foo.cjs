const sum = (n1, n2) => n1 + n2;

const sub = (n1, n2) => n1 - n2;

module.exports = {
  sum: sum,
  sub: sub,
};

// ================== OR ==================

// exports.sum = (n1, n2) => n1 + n2;
// exports.sub = (n1, n2) => n1 - n2;
